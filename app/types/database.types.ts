// Placeholder until `supabase gen types typescript --project-id <id>` is wired in.
// Until then queries are loosely typed; our `Profile`/`List`/etc. interfaces in
// `./db.ts` give us the static shapes we need.
export type Database = unknown
